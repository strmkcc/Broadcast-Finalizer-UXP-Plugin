[package]
name = "broadcast-finalizer-native"
version = "0.1.0"
edition = "2021"
license = "ISC"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = { version = "1.0.0", default-features = false, features = ["napi-6"] }
